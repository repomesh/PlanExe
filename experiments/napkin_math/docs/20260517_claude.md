# Pipeline status — Claude's view, 2026-05-17

Mid-PR snapshot. The previous status doc (2026-05-16) captured the
end of the five-prompt-rule phase, three plans cross-validated, the
deterministic Monte Carlo runner in production, and PR #707 about to
merge. This doc folds in the work since then: PR #708 (merged, five
new analysis blocks in `montecarlo.json`), PR #710 (in flight, the
insights.md restructure into a thin interpretation layer), and five
rounds of ChatGPT-driven format iteration (v34 → v38) that froze the
insights manifest at schema version 3.

The headline state: the pipeline now produces a **production-candidate
handoff artifact**. `insights.md` is no longer a project-manager
summary that duplicates the simulation tables — it is a navigation
and judgment layer over the intermediary artifacts, with a JSON
manifest, a provenance map, stable section names, and explicit trust
boundaries. The format has gone through five external review rounds
without finding structural breakage; the most recent (v38) review
called it *"production-candidate format; freeze the structure"*.

```
Cross-plan reference set (insights schema v3, 2026-05-17):

  v38/20260201_media_rescue           overall_risk_band: doom
  v38/20260516_datacenter_in_france   overall_risk_band: doom
  v33/20260514_cross_border_rail_ticketing  overall_risk_band: fragile
  v33/20250101_india_census           overall_risk_band: doom
  v33/20250720_faraday_enclosure      overall_risk_band: doom
  v31/20260215_nuuk_clay_workshop     overall_risk_band: doom

13 reference plans regenerate cleanly through the v3 renderer. No
structural drift across iterations; only the manifest's enum values
and one wording tweak per round.
```

The five new domains since 0516 — Hauts-de-France datacenter
(mega-infrastructure, EUR billion + km² + months mixed-unit gates),
containerized media rescue (global preservation program, mixed USD +
fraction gates), India census (public administration), Faraday
enclosure (commercial hardware), and the v33 rail update — each
produced a domain-appropriate model and rendered through the
interpretation-layer template without any per-plan adaptation. The
aggregation warning fires correctly on the plans whose gates use
incompatible units and no `min()` aggregate, and stays silent on the
rail plan where the `weakest_financial_gate_surplus_eur` min
aggregate is legitimate.

## TL;DR — what shipped between 0516 and 0517

Two PRs, one merged and one in flight.

**PR #708 — `feat/napkin-math-quick-wins` (merged):**
Five analysis blocks added to `run_monte_carlo.py`. Each block answers
a question the raw Monte Carlo tables don't:

| Block | Question answered | How |
|---|---|---|
| `binding_gate_analysis` | When a `min()` aggregate fails, which underlying gate was the binding constraint? | Per failed run, identify the dependency whose surplus is the smallest; aggregate frequency. |
| `quartile_analysis` | What is the lift in pass-probability from moving an input from its bottom quartile to its top quartile? | P(pass) conditioned on the input's quartile, top-bottom delta. |
| `required_input_thresholds` | What single-input restriction would lift a failing gate's pass rate to 80%? | Sweep input percentile cutoffs, find the tightest that satisfies the conditional pass probability ≥ 0.8. Empty list when no single-input fix is enough. |
| `missing_value_priority` | Which missing inputs are worth pinning down with real data, in priority order? | Score = `\|Δ-pp on worst gate\| × (1 − that gate's pass rate) × bound-width-ratio`. |
| `model_confidence` | How much should we trust each output? | HIGH/MEDIUM/LOW grade per output from the share of `source: data` input bounds and the average bound-width-to-base ratio. |

These five blocks become the structural data behind the new
`insights.md`: Decision implications uses `quartile_analysis`,
Failure drivers uses `quartile_analysis` + `required_input_thresholds`,
Missing inputs uses `missing_value_priority`, Per-output confidence
uses `model_confidence`, the aggregation footnote uses
`binding_gate_analysis`. The summarizer kept the old shape in #708
(still emitted the project-manager report); the structural shift came
in #710.

PR #708 also tightened a few reporting details:
- `np.ptp` replaces `np.std == 0` for constant-array detection
  (FP-precision bug).
- `min()` aggregate magnitudes are demoted after individual gates
  in verdict tables — the verdict is meaningful, the raw number is
  not.

**PR #710 — `feat/napkin-math-insights-as-handoff` (in flight, this branch):**
Reframes `insights.md` from a project-manager summary into an
**interpretation layer** over the intermediary artifacts. The user's
framing: "it's for downstream consumption, not humans skimming."
Subsequent rounds removed the "AI" labelling from the file itself —
the artifact describes what it *is* (a navigation/judgment layer)
rather than labelling its audience.

Five ChatGPT-driven format iterations, one commit each, no schema
churn within a round:

| Round | Insights schema | Changes |
|---|---|---|
| v34 → v35 | (pre-versioned) → v1 | Restructure: JSON manifest at top, provenance map, stable section names. Drop the per-output distribution, full quartile, and full sensitivity tables (they live in `montecarlo.json`). Aggregation warning fires when units are incompatible and no `min()` aggregate exists. Decision implications section added. `Source` column on missing-inputs renamed to `Basis` (`data` → `report_derived`, `assumption` → `model_assumption`). Open questions section added. JSON, not YAML, for the manifest. |
| v35 → v36 | v1 → v2 | `primary_model_result.label` renamed to `overall_risk_band` with a `basis` field stating it is the worst declared gate's band, not a whole-plan probability. `artifact_set` parsed from path (`version`/`plan_slug`/`relative_dir`). Gate verdicts gains a `Threshold basis` column (`report_explicit`/`report_inferred`/`model_defined`/`unknown`) from each gate's key_value `value_type`. Decision implications grows to 5 columns with a plan-specific column lifted from `parameters.recommended_first_calculations[].why_first`. `schema_notes` block with allowed enums. |
| v36 → v37 | v2 → v3 | Decision implications column renamed `Plan-specific revision hint` → `Gate meaning` (it was always rationale, not revision direction). DOOM/FRAGILE bullets switched from rhetorical ("the math says it almost never does") to dry ("fails X% of simulated runs under the current input bounds"). `Data-sourced inputs` confidence-table column renamed to `Declared-source inputs` with a footnote that neither declared-source nor model-assumption bounds are empirical real-world data. `basis_enum` widened to seven values (current pipeline still emits two; the rest reserve future provenance types). |
| v37 → v38 | v3 (no bump) | Two wording tweaks. "Almost certainly fails" in the DOOM band → "rarely passes under current bounds" (avoids epistemic overclaim). Decision implications intro line "the actual plan revision is for human or LLM interpretation against the source report" → "This section identifies the affected planning lever; concrete revisions should be derived by reading the source report and the relevant intermediary artifacts." |

ChatGPT's v38 verdict: *"freeze the structure. The format is mature
enough to test across many different report types. Further
structural iteration will probably produce churn rather than real
improvement."* Schema v3 is the frozen-candidate target.

The frozen section list:

```
# Insights: <plan name>
## Artifact contract
## Machine summary           (JSON manifest)
## Provenance map            (table of intermediary files)
## Modelling frame           (lifted from parameters.plan_summary.modelling_frame)
## Simulation settings
## Critical findings         (DOOM/FRAGILE bullets, scenario warns, model-collapse blanks, still-missing inputs)
## Gate verdicts             (with Threshold basis column; aggregation warning sub-section)
## Decision implications     (Gate | Verdict | Planning consequence | Structural lever | Gate meaning)
## Failure drivers           (one row per failing gate; binding-gate notes for aggregates)
## Missing inputs ranked by impact   (with Basis column)
## Confidence and trust boundaries   (Validated / Not validated / Per-output confidence)
## Scenario sanity check     (Low inputs / Base inputs / High inputs)
## Suggested next actions    (five imperatives)
## Open questions for next analysis pass   (five standing audit questions)
```

The Machine summary JSON shape (v3):

```json
{
  "insights_schema_version": 3,
  "artifact_type": "interpretation_layer",
  "plan_name": "...",
  "artifact_set": {"version": "v38", "plan_slug": "...", "relative_dir": "output/v38/..."},
  "source_plan_dir": "<absolute path; local-only>",
  "primary_model_result": {
    "basis": "worst declared gate's pass-rate band; not a calibrated whole-plan probability",
    "overall_risk_band": "doom",
    "reason": "...",
    "worst_gate": "...",
    "worst_gate_pass_rate": 0.0286
  },
  "validation_status": "valid",
  "simulation": {"n_runs": 10000, "seed": 12345, "distribution_default": "triangular"},
  "primary_failed_gates": [...],
  "primary_uncertainty_drivers": [...],
  "do_not_treat_as": [...],
  "schema_notes": { "...enums and the primary_model_result_semantics disclaimer..." }
}
```

## Cross-plan validation since 0516

The 0516 doc named cross-plan generalisation as *"substantively
closed, narrowly open"*. Two more domains and four more reference
iterations have been run since:

| Plan | Domain | Currency / Units | Notes |
|---|---|---|---|
| 20260516_datacenter_in_france | mega-infrastructure | EUR_billion, km2, months, fraction | Four independent gates, no `min()` aggregate. Aggregation warning fires. Schedule gate is `report_inferred`; others are `report_explicit`. |
| 20260201_media_rescue | global preservation program | USD_million, fraction | Five independent gates, no `min()` aggregate. Aggregation warning fires. |
| 20250101_india_census | public administration | (see artifact) | Schema v3 renders cleanly. |
| 20250720_faraday_enclosure | commercial hardware launch | EUR | Carried forward from v33; re-rendered under v3. |
| 20260514_cross_border_rail_ticketing | EU public infrastructure | EUR | Has a legitimate `min()` aggregate (`weakest_financial_gate_surplus_eur`); aggregation warning correctly suppressed. |
| 20260215_nuuk_clay_workshop | commercial small business | DKK | Carried forward from v31; re-rendered under v3. |

The aggregation-warning logic distinguishes correctly between the
rail plan (legitimate `min()` aggregate, no warning) and the
datacenter / media-rescue / Nuuk plans (mixed units, no aggregate,
warning fires). The `Threshold basis` column on Gate verdicts also
distinguishes correctly between the datacenter's `report_inferred`
schedule gate and its `report_explicit` land/financial/demand gates,
surfacing in the manifest a distinction the underlying
`parameters.json` had carried since v34 but the old insights.md did
not expose.

## What's open from the 0516 list

| Issue from 0516 | Status now |
|---|---|
| Compressed-vs-full-HTML head-to-head not run | **Still open.** The original headline question of the experiment. Six digest-extraction iterations on Nuuk, two on rail, one each on Faraday/datacenter/media-rescue — none compared head-to-head against `extract-parameters-from-full` on the same source. |
| Cross-plan generalisation | **Closed.** Five distinct domains across DKK + EUR + USD, four unit families. Schema v3 renders cleanly across all of them. |
| Validator/repair loop on parameters.json | **Partly addressed.** `validation.json` now exists and runs 16 structural checks (`json_parse`, `top_level_structure`, `required_fields`, `array_length_caps`, `global_id_uniqueness`, `snake_case_ids`, `depends_on_declared`, `formula_rhs_declared`, `fraction_value_range`, `comment_word_caps`, `source_text_word_caps`, `output_name_present_when_formula_hint`, `output_unit_present_when_formula_hint`, `no_dead_end_variables`, `threshold_friendly_naming`, `shared_pool_legitimacy`). Not yet a repair loop — fails the run rather than re-prompting. |
| `run-scenarios` still LLM-driven | **Unchanged.** Still the most expensive remaining LLM stage. |
| Compressor's role boundary load-bearing | **Unchanged.** Documented in two skill prompts and proposal 139; no automated check. |
| Insights model-collapse alert doesn't attribute cause | **Unchanged.** Critical findings bullets name the affected output but not the input combination that caused the blank. |
| Mixed-format digest's raw sections (~30 KB) | **Unchanged.** Worth measuring but not measured. |
| `output/` tree gitignored | **Unchanged.** Synthetic smoke fixture under `tests/fixtures/smoke/` is still the only canonical reference artifact in the repo. |

## Outstanding issues, in rough order of impact

### Highest leverage

1. **Compressed-vs-full-HTML head-to-head.** Unchanged from 0516. Most
   useful comparison: Nuuk Clay Workshop, where the digest extractor
   has now produced seven reviewed iterations (v27 → v33). Run the
   full-HTML extractor on the same source and diff the resulting
   `parameters.json` files.

2. **Lift `run-scenarios` into a Python runner.** The strict schema
   makes this a straight port. The current 13-reference-plan set has
   all needed `scenarios.json` files, but they were produced by an
   LLM stage that should now be deterministic.

3. **Format soak test across many different report types.** The v38
   reviewer recommended "test the format across many different
   report types" rather than further structural iteration. Worth
   running the pipeline on a deliberately heterogeneous batch
   (different currencies, mixed-unit gates, public-benefit +
   commercial + hobby/renovation domains) and looking for output
   sections that break or under-cover.

### Architectural

4. **Validator/repair loop.** The 16 structural checks in
   `validation.json` are run as fail-or-pass; on failure the
   upstream stage has to be re-run by hand. A small repair loop
   that fed structured violations back to the extractor would let
   us keep the strict schema without the manual re-prompt cycle.

5. **No regression test on the frozen v3 manifest.** The smoke test
   asserts a handful of section headings exist; it does not check
   the manifest's field set or the `schema_notes` enums. Worth
   adding a regression test on the synthetic fixture's `insights.md`
   that locks the manifest's top-level keys + the `schema_notes`
   enums + the section list, so any unintended drift in
   `summarize_insights.py` fails CI.

### Minor

6. **Model-collapse alert doesn't attribute cause.** Unchanged from
   0516. The Monte Carlo runner has the per-run pool state at the
   moment of the failure; could log which arguments were missing.

7. **`output/` tree gitignored.** Unchanged from 0516. Each format
   iteration regenerates 13 plans locally; none of them are
   reproducible without rerunning the upstream LLM stages. If the
   experiment is to be replicable by a new contributor, at least
   one reference plan's full output (parameters.json, bounds.json,
   scenarios.json, montecarlo.json, validation.json, insights.md)
   should be committed as a regression baseline.

8. **The schema-v3 `basis_enum` is wider than what the pipeline
   emits.** Documented as reserved for future provenance types
   (`report_explicit`, `report_inferred`, `external_reference`,
   `manual_override`, `unknown`); none of these are currently
   produced. If they stay unused for many iterations the
   schema-notes reservation will need to be honoured or removed.

## What I would do next, if continuing

In order:

1. **Run the head-to-head Nuuk comparison.** Original experimental
   question; still unanswered after seven digest iterations on the
   same source. The strict schema makes the diff easy to produce
   programmatically: same key_values set, same recommended_first_
   calculations set, same missing_values_to_estimate set, same
   shape of bounds. The interesting questions are the ones the
   diff cannot answer directly — does the full-HTML extractor pick
   up gates the digest extractor missed, or vice versa, and which
   has tighter bounds rationales.

2. **Add the manifest regression test.** Quickest win for v3
   stability. Synthetic fixture under `tests/fixtures/smoke/` plus
   a golden `insights.md` would catch any unintended manifest-shape
   drift in future edits.

3. **Lift `run-scenarios` to Python.** Mirrors what PR #705 did for
   Monte Carlo. The 13-plan reference set already has the scenarios
   files; a Python runner would let us regenerate them
   deterministically and surface the per-scenario warnings the
   current LLM stage produces with variable recall.

4. **Run the head-to-head on a second plan (rail or Faraday) if
   #1 returns a definitive answer.** Single-plan comparisons can
   tip either way for compression-specific reasons; a second
   comparison on a different domain disambiguates.

## Closing thought

The 0516 doc closed on *"the original headline question of the
napkin_math experiment is now the single biggest remaining gap"*.
That is still true. What the day between has added is a
production-candidate output format on the downstream side: the
pipeline can now emit a stable, machine-readable, externally-reviewed
interpretation layer that a programmatic consumer can read without
parsing prose. The cross-plan generalisation question is
substantively closed. The format-design question is closed at v3.
The compressor-vs-full-HTML question is the next experiment to run,
and the natural next session.
