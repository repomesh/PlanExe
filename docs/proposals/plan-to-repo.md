# Plan-to-Repo Proposal

## Summary

Every plan that PlanExe generates should automatically provision a GitHub repository. This repository becomes the persistent, versioned home for that plan and serves as the canonical handoff point for AI agents and humans to continue development, iteration, and execution.

## Why

**Persistent Starting Point:** AI agents (and humans) need a structured, version-controlled home for each plan. Without a repo, agents have no shared baseline to fork from, improve, or build upon.

**Execution Continuity:** The plan repo IS the plan's home. Future agents can fork it, submit PRs with improvements, execution results, or modifications. It creates a natural workflow:
- Agent A generates plan → creates repo
- Agent B picks up the repo → builds website → PRs changes
- Agent C picks up the updated repo → executes plan → PRs execution results
- Humans review and merge improvements

**Traceability:** Every modification to the plan is version-controlled, auditable, and reversible.

## What Lands in the Repo

1. **Plan Artifact**
   - Generated plan in Markdown or JSON format
   - Timestamped and signed by the generating agent

2. **README**
   - Summary of the plan's purpose and goals
   - Next steps and immediate actions
   - Links to related documents or repos

3. **Folder Structure**
   - `/website` — for web-based presentation, design, or deployment files
   - `/execution` — for execution scripts, logs, and progress tracking
   - `/docs` — for detailed specifications, requirements, and notes

4. **Metadata**
   - `.planexe.yml` or similar config: plan ID, generator, creation timestamp, owner

## API Design Sketch

### Endpoint: Create Repo on Plan Completion

When a plan is generated, PlanExe triggers a new endpoint or webhook:

```
POST /api/v1/plans/:planId/create-repo
```

**Payload:**
```json
{
  "planId": "plan-uuid",
  "planTitle": "Website Redesign Q1 2026",
  "planContent": "...",
  "planFormat": "markdown",
  "repoName": "plan-website-redesign-q1-2026",
  "repoDescription": "Auto-generated plan repo",
  "isPrivate": false,
  "githubToken": "ghp_...",
  "ownerOrg": "VoynichLabs"
}
```

**Actions:**
1. Call GitHub API (`POST /user/repos` or `POST /orgs/{org}/repos`)
2. Initialize repo with plan files (plan artifact, README, folder scaffold)
3. Create initial commit with plan content
4. Return repo URL and clone instructions
5. Store repo URL in PlanExe database (linked to `planId`)

### Auth & Token Management

- **Per-User GitHub Token:** User supplies personal GitHub token (in environment, config file, or per-session prompt)
- **Config Storage:** Store token in PlanExe config (encrypted, with user consent)
- **Scope Requirements:** Token needs `public_repo` or `repo` scope (depending on public/private setting)
- **Org Token Alternative:** For business plans, org provides service account token

### Scenarios

#### Scenario 1: Open Source Plan
- User generates plan for open source project
- Repo created as **public**
- Plan is discoverable, others can fork and contribute
- Plan repo becomes community reference

#### Scenario 2: Business/Private Plan
- User generates plan for internal initiative
- Repo created as **private**
- Plan is internal-only
- Agents can still fork, branch, and submit PRs within the org

## Future: Agent Workflow

```
┌─────────────────┐
│ PlanExe         │ Generates plan
│ generates plan  │ → creates repo
└────────┬────────┘
         │
         v
    [plan repo on GitHub]
         │
    ┌────┴────┬──────────┬─────────┐
    │          │          │         │
    v          v          v         v
  [Agent A]  [Agent B]  [Agent C]  [Human]
  (fork)     (fork)     (fork)     (review)
    │          │          v         │
    │          │      Execute       │
    │          v        plan        v
    │       Build      & submit    Merge
    │       website    results      changes
    │         & PR
    │         │
    └─────────┴─────────┬──────────┘
                        v
              [Updated Plan Repo]
              (versioned, auditable,
               improvement history)
```

## Implementation Considerations

1. **Error Handling**
   - If GitHub API fails, gracefully degrade (log error, notify user, allow manual repo creation)
   - Validate token before attempting repo creation

2. **Rate Limiting**
   - GitHub API has rate limits; queue or batch repo creation if needed
   - Provide feedback on quota status

3. **Naming Strategy**
   - Repo name: `plan-{slugified-title}-{timestamp}` or similar
   - Ensure uniqueness (check for existing repos)
   - User can override repo name if desired

4. **Initial Commit Message**
   - `Initial commit: Auto-generated by PlanExe`
   - Include plan metadata and generation timestamp

5. **Future Integrations**
   - Link to PlanExe UI: clickable "View Repo" button on generated plans
   - Webhooks: sync plan updates back to PlanExe if repo is modified
   - Agent SDK: provide helper function for agents to fork and submit PRs

## Success Criteria

- [x] Plan generation triggers automatic repo creation
- [x] Repo contains plan artifact, README, and scaffold folders
- [x] User can specify public/private
- [x] User supplies GitHub token securely
- [x] Agents can fork, branch, and submit PRs to plan repo
- [x] Plan repo URL is accessible and linked in PlanExe UI
- [x] Version history is preserved and auditable

## Open Questions

1. Should we auto-create issues/labels for tracking plan execution?
2. Should plan updates trigger repo commits/tags?
3. How do we handle plan deletion (repo archival or full deletion)?
4. Should we support GitLab, Gitea, or other Git hosting platforms?
